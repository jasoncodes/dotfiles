[user]
# These details are used for commits and default to your local machine's user account
# name = John Appleseed
# email = me@example.com

[github]
# These details can be found at <https://github.com/account>
# user = USERNAME
# token = TOKEN

[color]
diff = auto
branch = auto
status = auto
ui = auto

[core]
# git doesn't appear to support shell expansions here
# you'll have to replace this with the full path to your .gitignore
excludesfile = ~/.gitignore

[alias]
st = status
ci = commit
co = checkout
br = branch

[push]
default = tracking
